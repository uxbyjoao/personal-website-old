import { Element as ScrollElement } from "react-scroll";

import Heading from "../Heading";

export default function Projects() {
  return (
    <ScrollElement name="projects" className="pt-8">
      <p>todo</p>
    </ScrollElement>
  );
}
